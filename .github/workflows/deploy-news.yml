# CI/CD: GitHub Actions → Docker Hub (News Service)
# EC2 배포 없음. EC2에서 Watchtower가 자동 pull.
#
# 모노레포: backend/news.kroaddy.site/ 변경 시에만 실행

name: Build & Push News to Docker Hub

on:
  push:
    branches: [main]
    paths:
      - 'backend/news.kroaddy.site/**'
      - '.github/workflows/deploy-news.yml'
  workflow_dispatch:

env:
  DOCKER_IMAGE: necromancer1234/news.kroaddy.site

jobs:
  build-and-push:
    name: Build & Push to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend/news.kroaddy.site
          file: ./backend/news.kroaddy.site/Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE }}:${{ github.sha }}
            ${{ env.DOCKER_IMAGE }}:latest
          cache-from: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache
          cache-to: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache,mode=max
